export async function recordTradeOnChain(
  wallet: any,
  tradeData: any
) {
  // Отключаем blockchain пока что
  return null;
}

export async function verifyTrade(signature: string) {
  return true;
}
